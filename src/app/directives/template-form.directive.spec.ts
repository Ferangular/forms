import { TemplateFormDirective } from './template-form.directive';

describe('TemplateFormDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateFormDirective();
    expect(directive).toBeTruthy();
  });
});
