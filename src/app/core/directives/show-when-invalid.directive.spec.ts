import { ShowWhenInvalidDirective } from './show-when-invalid.directive';

describe('ShowWhenInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowWhenInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
