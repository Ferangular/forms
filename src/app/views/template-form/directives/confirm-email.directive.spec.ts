import { ConfirmEmailValidator } from './confirm-email-validator.directive';

describe('ConfirmEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEmailValidator();
    expect(directive).toBeTruthy();
  });
});
