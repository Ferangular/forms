import { BannedWordDirective } from './banned-word.directive';

describe('BannedWordDirective', () => {
  it('should create an instance', () => {
    const directive = new BannedWordDirective();
    expect(directive).toBeTruthy();
  });
});
